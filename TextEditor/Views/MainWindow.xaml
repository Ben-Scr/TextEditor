<Window x:Class="TextEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
       xmlns:controls="clr-namespace:TextEditor.Views.Controls"
        xmlns:local="clr-namespace:TextEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent" Foreground="{StaticResource Foreground.Primary}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44"
                           CornerRadius="10"
                           UseAeroCaptionButtons="False"
                           ResizeBorderThickness="5"/>
    </shell:WindowChrome.WindowChrome>

    <Border CornerRadius="10"
           Background="{StaticResource Background.Subtle}"
           BorderBrush="{StaticResource Border.Default}"
           BorderThickness="1"
           SnapsToDevicePixels="True">



        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>

            <controls:TitleBar Margin="0 -5 0 0"/>

            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="Select File" 
    VerticalAlignment="Stretch" 
    HorizontalAlignment="Stretch"
    Click="SelectDirectory_Click"
                           Padding="5"
    Style="{StaticResource DefaultButton}"/>
                    <Button Content="Save File"
        Name="ButtonSaveFile"
        VerticalAlignment="Stretch"
        HorizontalAlignment="Stretch"
        Click="SaveFile_Click"
                            Padding="5"
        Style="{StaticResource DefaultButton}"/>

                    <Label Name="SelectedFileName" Foreground="{StaticResource Foreground.Secondary}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10 0 0 0"/>

                </StackPanel>
            </Grid>

            <Grid Grid.Row="2">
                <Label Name="LineCountText" Foreground="{StaticResource Foreground.Secondary}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10 0 0 0"/>
                <Label Name="LettersCountText"  Foreground="{StaticResource Foreground.Secondary}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10 0 0 0"/>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="23*"/>
                    <ColumnDefinition Width="110*"/>
                </Grid.ColumnDefinitions>
                <TextBox Style="{StaticResource DefaultTextBox}"
                         WindowChrome.IsHitTestVisibleInChrome="True"
                         Padding="5"
                         FocusVisualStyle="{x:Null}"
                         Name="FileContentTextBox" TextChanged="OnTextChanged"
                         TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.ColumnSpan="2"/>
            </Grid>
        </Grid>
    </Border>
</Window>
